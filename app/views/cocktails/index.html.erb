<div class="title">
  <h1>All cocktails</h1>
</div>

<% @cocktails.each do |cocktail| %>
<div class="cocktail-card">
  <img src="https://picsum.photos/300">
  <strong><%= cocktail.name %></strong>
  <%= link_to "See details", cocktail_path(cocktail.id), class: "btn btn-primary" %>
<% end %>
</div>
